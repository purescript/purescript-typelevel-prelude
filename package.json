{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "pulp build -- --censor-lib --strict"
  },
  "devDependencies": {
    "pulp": "^12.0.1",
    "purescript-psa": "^0.6.0",
    "rimraf": "^2.6.1"
  }
}
